<nav class="fp-pagination" aria-label="{{ ucfirst($recordLabel) }} pagination"><p>{{ number_format($paginator->firstItem() ?? 0) }}–{{ number_format($paginator->lastItem() ?? 0) }} of {{ number_format($paginator->total()) }} {{ $recordLabel }}</p><div>@if($paginator->previousPageUrl())<a class="module-button" rel="prev" href="{{ $paginator->previousPageUrl() }}">Previous</a>@endif @if($paginator->nextPageUrl())<a class="module-button" rel="next" href="{{ $paginator->nextPageUrl() }}">Next</a>@endif</div></nav>
