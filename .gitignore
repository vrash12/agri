/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Local operator notes that have held account credentials. Never commit these.
/credentials.txt
/credentials*.txt
*.credentials

# Generated user manuals and branding exports. Regenerated from source, so the
# repository tracks what produces them rather than the output itself.
/output
